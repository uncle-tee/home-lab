apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-native-operator.fullname" . }}
  labels:
    {{- include "cloud-native-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloud-native-operator.selectorLabels" . | nindent 4 }}
